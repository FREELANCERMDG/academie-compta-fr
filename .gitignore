# Dépendances & runtime
platform/node_modules/
platform/.env
platform/data.db
platform/data.db-shm
platform/data.db-wal
platform/screenshots/

# Python
**/__pycache__/
*.pyc

# Temporaire
*.tmp

# Privé (mots de passe / accès) — NE JAMAIS publier
PENSE-BETE-ACCES.md

# Deploy hook Render (secret) — NE PAS publier
.deploy-hook
